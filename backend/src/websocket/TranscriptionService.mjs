class TranscriptionService {
    // Define a method signature for transcribing audio
    async transcribe(audioData, options = {}) {
        throw new Error('Transcribe method not implemented');
    }
}

export default TranscriptionService;